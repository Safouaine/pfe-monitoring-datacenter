# Web Platform Deep Dive

## Overview
The `web-platform` module surfaces the monitoring and predictive maintenance results to end-users over a web interface or REST API. Built on FastAPI, the service currently exposes an entrypoint for routing and status checks.

## Current Implementation
The `web-platform/main.py` file initializes a minimal FastAPI application.

### Key Features
- **FastAPI Core**: Utilizes the powerful, asynchronous Python framework `FastAPI` (via `from fastapi import FastAPI`).
- **Root Endpoint (`/`)**: Currently implemented as a health check and status confirmation. 
    - Function: `read_root()`
    - Output: `{"status": "Plateforme de Monitoring Nouvameq"}`
    - Access: Configured to map the container port `8000` to localhost port `8000` via Docker Compose.

### Execution Context
- Uses default generic container setups (via a custom Python image expected in its internal `Dockerfile`) and starts the FastAPI server presumably using a web server such as `uvicorn` (often present in a requirements.txt file alongside FastAPI).

## Future Development Integration
To serve its purpose fully, changes expected for this module:
- Integrate with **InfluxDB** to display raw temperature/sensor data fetched by the `snmp-collector`.
- Integrate and visualize predictions, faults, or alerts generated by the `ai-engine`.
- Integrate with **Grafana** (configured in docker-compose.yml at `localhost:3000`) by creating iframe embeds or direct webhooks to present analytical dashboards.
